import React from "react";
import { Container } from "react-bootstrap";

const CreateEmployee = () => {
  return (
    <Container className="mt-4">
      <h1>Create Employee</h1>
      <p>Form to add new employees will go here.</p>
    </Container>
  );
};

export default CreateEmployee;
